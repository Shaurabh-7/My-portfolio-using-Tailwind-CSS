{
  "buildCommand": "chmod +x ./node_modules/.bin/tailwindcss && npm run build",
  "outputDirectory": "src",
  "rewrites": [
    { "source": "/images/:path*", "destination": "/images/:path*" },
    { "source": "/app.js", "destination": "/app.js" }
  ]
}
